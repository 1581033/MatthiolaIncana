<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.matth.mapper.PaginationMapper">

    <select id="selectObjsByPage" resultType="map">
        select ${ew.sqlSelect} from ${sq} ${ew.customSqlSegment}
    </select>

    <select id="selectObjsByList" resultType="map">
        select ${ew.sqlSelect} from ${sq} ${ew.customSqlSegment}
    </select>

</mapper>
